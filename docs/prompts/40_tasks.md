# 40 — /speckit.tasks 프롬프트

/speckit.tasks

추가 지시(필요 시):
- tasks는 “작고 검증 가능”하게 쪼개라.
- 먼저 baseline(동적 퍼텐셜 + link queue)로 데모를 만든 뒤,
  bandit 학습, 그 다음에 GNN/LSTM을 플러그인으로 추가하라.
- 각 task에: 수정 파일 경로, 완료 조건(테스트/스모크), 성능 체크를 적어라.
